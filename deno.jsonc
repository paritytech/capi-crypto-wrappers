{
  "tasks": {
    "wasmbuild": "deno run -A https://deno.land/x/wasmbuild@0.10.2/main.ts",
    "build": "deno task wasmbuild --out lib/sync --sync && deno task wasmbuild --out lib/fetch && deno run -A _build.ts"
  }
}
